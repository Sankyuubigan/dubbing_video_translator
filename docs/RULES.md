# Тестовые выходные файлы — только в папку test/
Любые файлы, создаваемые в процессе тестирования (output, логи, промежуточные данные), должны сохраняться ТОЛЬКО в подпапку test/ проекта. Запрещается создавать их в корне проекта.
